// semantic types
export namespace s {
  export type Milliseconds = number;
}

export enum ResourceNames {
  grain = 'grain',
  minerals = 'minerals',
  knowledge = 'knowledge',
}

export enum BuildingNames {
  farm = 'farm',
  mine = 'mine',
  lab = 'lab',
}
